#! /usr/bin/env python
# encoding: utf-8

def build(bld):
  bld(features = 'cxx cxxprogram',
      source   = 'tim.cc',
      target   = 'tim',
      use      = [ 'project-headers', '3rd-party', 'BENCHMARK' ])
