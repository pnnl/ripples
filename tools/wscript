#! /usr/bin/env python
# encoding: utf-8

def build(bld):
  bld(features = 'cxx cxxprogram',
      source   = 'influence_maximization.cc',
      target   = 'influence-maximization',
      use = [ 'project-headers', 'BOOST' ])
